设备：GPU：NVIDIA 5060 Laptop 8.0GB显存 115W功耗 电源适配器100W
系统：Windows 11 Pro RAM：32GB（31.4GB usable）

测评模型：1.QIRA私有模型（通过API访问）baseline 2.国内开源模型（本地部署） 3.国外开源模型（本地部署） 注：本地模型需兼容边缘设备（PC/Andorid）部署与推理。
Gemma 4-E4B  Phi-4-mini  Qwen-3.5 4B  GLM-edge-4B  （均可达到128K context window）

测评目标：对比不同模型在各项指标下的压缩效果与性能影响。

边际信息增益 (Marginal Information Gain) = Σ(保留token的信息熵) - Σ(删除token的信息熵)

测评指标：
	1. 信息保真度：
		困惑度变化率（PPL Change）： 衡量压缩文本是否破坏了语言模型的原生概率分布。
		关键信息召回率（Recall）： 采用“关键事实提问”计算召回百分比。
		语义相似度（Cosine Similarity）： 利用 Embedding 模型对比原始输出与压缩后输出的向量距离。
		BARTScore：判断上下文压缩前后模型响应质量变化。
	2. 性能与效率：
		压缩延迟： 包含压缩算法本身的计算耗时（这是边缘设备能否接受的关键）。
		首Token响应时间 (TTFT)： 重点对比压缩后的 Prefill 阶段加速比。
		推理吞吐量 (TPS)： 观察 Token 减少后对生成速度的提升。
		显存/内存占用： 记录峰值（Peak RAM/VRAM），评估对移动端的压力。
		压缩密度增益： 单位 Token 承载的信息量提升比。
		KV Cache 减少率： 内存占用下降情况。  

测评场景：
	1. 长文档压缩（优先级下降）
	2. QA多轮对话
	3. 工具调用，中间状态

待确认：
	上下文需要保留哪些信息（例如工具调用记录、结果等）
	是否考虑多模态拓展（附件）（mime type）
	是否需要约束一个最大的context window  (32K)
	上下文压缩管理的方案（会话级,一个session一个）

TASK1：
制定测评计划（上下文压缩任务中使用本地模型与云端模型作为压缩器，从latency和acc角度的对比），包括本地模型选型，指标选择，测评场景的模拟。
选择Gemma 4-E4B  Phi-4-mini  Qwen-3.5 4B  GLM-edge-4B四个模型，其中gemma4-E4B和Phi-4-mini为美国开源模型（Tiny Models）中综合表现较好，Qwen-3.5-4B和GLM-edge-4B为中国开源模型（Tiny Models）中综合表现较好。同时可满足大部分pc设备的本地部署和推理。
测评指标会从压缩质量和压缩器本身的性能两个方面出发。压缩质量方面选取：PPL Change、Recall、Cosine Similarity、BARTScore四个指标综合判断；
压缩器本身的性能方面选取：压缩延迟（s/10k tokens）、首TOKEN响应时间、推理吞吐量（tokens/s）、显存/内存占用、压缩密度增益、KV Cache减少率六个指标综合判断。
测评场景会以多轮对话+工具调用场景为主，主要关注长对话场景中在维持一个有限长度上下文的前提下，本地模型对关键信息的总结与记忆能力。
实验设备配置：设备：GPU：NVIDIA 5060 Laptop 8.0GB显存 115W功耗 电源适配器100W 系统：Windows 11 Pro RAM：32GB（31.4GB usable）
压缩级别（会话级）